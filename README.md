clrun
=====

shell script to setup test parameters / test environment for curl-loader launching

1. download and compile curl-loader;
2. setup dns server to do dns spoofing for any given dns queries;
3. setup return routes for simulated private IP addreses;
4. tune Linux kernel's parameters to launch curl-loader;
